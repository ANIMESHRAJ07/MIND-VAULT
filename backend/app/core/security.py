"""Password hashing and JWT helper functions will live here."""

# TODO:
# - hash_password(password)
# - verify_password(plain_password, hashed_password)
# - create_access_token(data)
# - decode_access_token(token)
